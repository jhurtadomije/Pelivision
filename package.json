{
  "name": "pelivision",
  "version": "1.0.0",
  "description": "Descripción de tu proyecto",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "mkdir -p build && cp -r * build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
