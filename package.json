{
  "name": "pelivision",
  "version": "1.0.0",
  "description": "Descripción de tu proyecto",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
   "build": "rm -rf build && mkdir build && cp -r * build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
